@model List<questions>
@{
    ViewData["Title"] = "Search";
}

<h1>Search</h1>

@foreach (questions quest in Model)
{
    <div>@quest.ID @quest.Username @quest.Detail @quest.QuestionsID @quest.Posted @quest.Tags @quest.Title</div>
}
